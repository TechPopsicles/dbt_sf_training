python --version
Install Python 3.12 using pip

python -m venv 
source venv/bin/activate

pip install dbt-core==1.11.8 dbt-snowflake==1.11.4

dbt init
dbt run --select <model_name>
dbt run -s <model_name>
dbt test
dbt test -s <model_name>
dbt empty
dbt docs generate
dbt docs serve
dbt seed
dbt ls
dbt ls --resource-type model
dbt ls --resource-type test
dbt ls --resource-type seeds
dbt ls --resource-type seed
dbt build -m <model_name> (deprecated in favor of --select -s)
dbt deps
-- debug
using logs folder

-- Deep dive
using target dir and look into compile and run folders

kill -9 $(lsof -t -i:8080)

dbt compile
copy the target/manifest.json to state/
dbt run -m <model_name> --defer --state state

dbt clone --state state